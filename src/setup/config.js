export const config = {
    ENTITY: "item",
    ENDPOINT: "/items",
    SELECTOR: "#items", // The selector you want to use for the DOM element. We recommend ID: "#addresses"
    DEFAULT_LOCALE: "en" // The default language of the widget
};