export const config = {
    ENTITY: "address",
    ENDPOINT: "/addresses",
    SELECTOR: "#addresses", // The selector you want to use for the DOM element. We recommend ID: "#addresses"
    DEFAULT_LOCALE: "en" // The default language of the widget
};